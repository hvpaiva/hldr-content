kind: Collection
metadata:
  name: projects
spec:
  kind: Project
  enabled: true
  description: Work worth opening. Context lives on the project page, not the README.
  order: highlight
  badge: status
  columns:
    - field: status
      width: 9
    - field: tags
      header: stack
