# Every 404 shows this under the lines that say what was not found.
kind: Page
metadata:
  name: not-found
  title: "404"
spec:
  content:
    inline: |
      {{ link / README.md }}      start here
      {{ link /projects projects/ }}      everything indexed
      {{ link /help help.txt }}       keys and commands
